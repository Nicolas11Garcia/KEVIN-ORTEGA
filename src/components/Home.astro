---
import { Button } from "@/components/ui/button";
---

<section class="min-h-screen relative flex w-full">
  <div class="bg-[#f8e28f] w-full"></div>
  <div class="bg-[#d1d1d1] w-full"></div>

  <!--Centro-->
  <div class="absolute inset-0 flex justify-center">
    <div class="mt-16">
      <h1 class="font-semibold text-5xl text-negro">Meet the Sayles Twins</h1>
      <div class="bg-white mt-6 p-2 -rotate-2 max-w-fit mx-auto">
        <p class="text-negro text-center">
          Bert uses a <span class="text-yellow font-medium">Mobilo Card</span>,
          but Ernie is stuck in
        </p>
      </div>
      <div class="bg-white mt-2 p-2 -rotate-2 max-w-fit mx-auto">
        <p class="text-negro text-center">
          the past using <span class="text-yellow font-medium"
            >paper business cards</span
          >
        </p>
      </div>

      <div class="w-full max-w-[528px] mt-4">
        <p class="text-negro text-center text-balance">
          While Bert hits his sales numbers, Ernie misses his targets and gets
          fired. Moral of the story? Make the switch from paper business cards
          to Mobilo!
        </p>
      </div>

      <div class="mx-auto flex gap-4  max-w-fit mt-8"> 
        <Button className="bg-negro">Llamado Acción</Button>
        <Button className="text-negro" variant="ghost">Llamado Acción</Button>
      </div>
    </div>
  </div>
</section>
